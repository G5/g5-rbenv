# Set up rbenv in the shell
export RBENV_ROOT=<%= @rbenv_root %>
export PATH=$RBENV_ROOT/bin:<%= @ruby_build_bin_path %>/bin:$PATH
. $RBENV_ROOT/completions/rbenv.bash
eval "$(rbenv init -)"

# Favor local binstubs over rbenv shims (this must be
# executed *after* rbenv init
export PATH=bin:$PATH
